<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="StarlightGUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:essential="using:WinUI3Package"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Starlight GUI"
    mc:Ignorable="d">

    <Grid x:Name="MainWindowGrid">
        <Grid Background="{StaticResource WindowBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.Resources>
                <essential:ModernStandardWindowContextMenu x:Name="Menu" Window="{x:Bind}" />
            </Grid.Resources>

            <!--  自定义标题栏  -->
            <Grid
                x:Name="AppTitleBar"
                Height="48"
                Background="{StaticResource WindowBackgroundBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--  应用图标和标题  -->
                <StackPanel
                    Grid.Column="0"
                    Margin="16,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <Image
                        Width="24"
                        Height="24"
                        Margin="0,4,0,0"
                        Source="/Assets/Starlight.png" />
                    <TextBlock
                        Margin="16,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                        Text="Starlight GUI" />
                    <TextBlock
                        Margin="4,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="13"
                        Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                        Text="{StaticResource Version}" />
                </StackPanel>

                <!--  标题栏拖拽区域  -->
                <Grid
                    x:Name="DragRegion"
                    Grid.Column="1"
                    Background="Transparent" />

                <!--  窗口控制按钮  -->
                <essential:WindowCaptionButtonThemeWorkaround Window="{x:Bind}" />
            </Grid>

            <!--  侧边栏区域  -->
            <NavigationView
                x:Name="RootNavigation"
                Grid.Row="1"
                Background="{StaticResource WindowBackgroundBrush}"
                CompactModeThresholdWidth="0"
                ExpandedModeThresholdWidth="0"
                IsBackButtonVisible="Collapsed"
                IsPaneOpen="False"
                IsPaneToggleButtonVisible="True"
                IsSettingsVisible="True"
                ItemInvoked="RootNavigation_ItemInvoked"
                OpenPaneLength="280"
                PaneDisplayMode="LeftCompact">

                <!--  选项，从上到下  -->
                <NavigationView.MenuItems>
                    <NavigationViewItem
                        Content="主页"
                        Icon="Home"
                        Tag="Home" />
                    <NavigationViewItem Content="任务管理" Tag="Task">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xf120;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem Content="模块管理" Tag="KernelModule">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xe8ce;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem Content="文件管理" Tag="File">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xec50;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem Content="窗口管理" Tag="Window">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xe737;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem Content="系统功能" Tag="Utility">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xec7a;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem Content="监视器" Tag="Monitor">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xea8f;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem Content="内存反汇编" Tag="Disasm">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xe943;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                </NavigationView.MenuItems>

                <!--  从上到下  -->
                <!--  最下面是设置  -->
                <NavigationView.FooterMenuItems>
                    <NavigationViewItem
                        Content="Work in progress..."
                        IsEnabled="False"
                        Tag="Deuterium">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xe945;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem
                        Content="关于"
                        Icon="Help"
                        Tag="Help" />
                </NavigationView.FooterMenuItems>

                <!--  内容  -->
                <Frame
                    x:Name="MainFrame"
                    Grid.Row="1"
                    CacheSize="4">
                    <ProgressRing
                        x:Name="LoadingRing"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        IsActive="True" />
                </Frame>
            </NavigationView>
        </Grid>
        <StackPanel
            x:Name="InfoBarPanel"
            Margin="0,58,10,0"
            HorizontalAlignment="Right" />
    </Grid>
</Window>